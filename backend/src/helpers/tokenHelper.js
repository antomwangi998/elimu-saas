// ============================================================
// tokenHelper — JWT token generation and validation
// ============================================================
'use strict';

/**
 * JWT token generation and validation
 */

// ── Exports ────────────────────────────────────────────────────
module.exports = {
  // JWT token generation and validation
};
